<form action="/roles/edit/<%= role.id %>" method="POST">
    <div class="mb-3">
      <input type="text" value= "<%= role.descripcion %>" class="form-control" name="description" placeholder="Descripción">
    </div>
    <div class="mb-3">
      <% for (let ruta of rutas) { %>
        <% if ( arr.includes(ruta.id)) { %> 
          <div class="form-check form-check-inline">
            <input class="form-check-input" type="checkbox" id="inlineCheckbox1" name="check" value="<%= ruta.id %>" checked>
            <label class="form-check-label" for="inlineCheckbox1"><%= ruta.descripcion %></label>
          </div>
        <%  } else { %>
          <div class="form-check form-check-inline">
            <input class="form-check-input" type="checkbox" id="inlineCheckbox1" name="check" value="<%= ruta.id %>">
            <label class="form-check-label" for="inlineCheckbox1"><%= ruta.descripcion %></label>
          </div>
        <%  } %>
      <% } %> 
    </div>
    <div class="mb-3">
      <button class="btn btn-success btn-block">
        Guardar
      </button>
    </div>
</form>