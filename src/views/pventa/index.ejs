<div class="container-fluid px-4">
  <h1 class="mt-4">Puntos de venta</h1>
  <ol class="breadcrumb mb-4">
      <li class="breadcrumb-item"><a href="index.html">Dashboard</a></li>
      <li class="breadcrumb-item active">Puntos de venta</li>
  </ol>
  <%- include('../partials/message') %>
  <div class="card mb-4">
      <!-- <div class="card-header">
          <i class="fas fa-table me-1"></i>
          DataTable Users
      </div> -->
      <div class="card-body">
          <button type="button" class="btn btn-primary btn-header" data-bs-toggle="modal" data-bs-target="#addModal">
              Añadir punto de venta
          </button>
          <table id="datatablesSimple">
              <thead>
                  <tr>
                      <th>Código</th>
                      <th>Nombre</th>
                      <th>Tipo</th>
                      <th>Descripción</th>
                      <th>Sucursal</th>
                      <th>Creado</th>
                  </tr>
              </thead>
              <tfoot>
                  <tr>
                      <th>Código</th>
                      <th>Nombre</th>
                      <th>Tipo</th>
                      <th>Descripción</th>
                      <th>Sucursal</th>
                      <th>Creado</th>
                  </tr>
              </tfoot>
              <tbody>
                  <% for (let resultado of resultados) { %>
                      <tr>
                          <td><%= resultado.codigo %></td>
                          <td><%= resultado.nombre_pventa %></td>
                          <td><%= resultado.tipo_pventa %></td>
                          <td><%= resultado.descripcion %></td>
                          <td><%= resultado.sucursal_id %></td>
                          <td><%= resultado.creado.toLocaleString() %></td>
                          <td>
                              <button data-id="<%= resultado.id %>" data-name="puntoventa" type="button" class="btn btn-primary editbtn" data-bs-toggle="modal" data-bs-target="#editModal">
                                  Editar
                              </button>
                              <a data-id="<%= resultado.id %>" data-name="puntoventa" class="btn btn-danger btn-delete">Eliminar</a>
                          </td>
                      </tr>
                  <% } %> 
              </tbody>
          </table>
      </div>
  </div>
</div>
<script src="/js/crud.js"></script>
<%- include('./add_modal', sucursales) %>

<div class="modal fade" id="editModal" tabindex="-1" aria-labelledby="addModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
      <div class="modal-content">
          <div class="modal-header">
              <h5 class="modal-title" id="addModalLabel">Editar punto de venta</h5>
              <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
          </div>
          <div id="editModalBody" class="modal-body">
              
          </div>
      </div>
  </div>
</div>